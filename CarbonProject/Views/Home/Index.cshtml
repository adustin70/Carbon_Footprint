@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Carbon Footprint?</h1>
    <p>How can carbon have a footprint if it doesn't have feet?</p>
</div>

<script src='https://api.tiles.mapbox.com/mapbox-gl-js/v0.53.0/mapbox-gl.js'></script>
<link href='https://api.tiles.mapbox.com/mapbox-gl-js/v0.53.0/mapbox-gl.css' rel='stylesheet' />
<script src="https://unpkg.com/deck.gl@7.3/dist.min.js"></script>

<div id="container" style="height: 500px;"></div>

@*<script type="text/javascript" src="~/js/heatMap.js"></script>*@
@*Don't forget to uncomment this ^^ needed for awesome map*@

<div class="chart-container" style="position: relative">
    <canvas id="average" width="300" height="150" aria-label="Carbon" role="img"></canvas>
</div>
<script src="https://cdn.jsdelivr.net/npm/chart.js@2.9.2/dist/Chart.min.js"></script>

<script>
    const api_url = 'https://api.v2.emissions-api.org'+ '/api/v2/carbonmonoxide/average.json'
        + '?country=US&begin=2021-02-01&end=2021-03-01'

    window.onload = function () {
        fetch(api_url)
            .then(response => response.json())
            .then(data => {
                let ctx = document.getElementById('average').getContext('2d');
                new Chart(ctx, {
                    type: 'bar',
                    data: {
                        labels: data.map(x => x.start.substring(8, 10)).sort(),
                        datasets: [{
                            label: 'America',
                            backgroundColor: '#93bd20',
                            data: data.map(x => x.average),
                        }]
                    },
                    options: {
                        scales: {
                            yAxes: [{
                                scaleLabel: {
                                    display: true,
                                    labelString: 'carbon monoxide [mol/m²]'
                                },
                                ticks: {
                                    beginAtZero: true
                                }
                            }],
                            xAxes: [{
                                scaleLabel: {
                                    display: true,
                                    labelString: 'day'
                                }
                            }]
                        }
                    }
                });
            })
    }
</script>